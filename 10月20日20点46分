#include<stdio.h>
#include<string.h>
int main()
{
	char str_len(char str[]);
	char str[99];
	gets(str);
	printf("%d",str_len(str));
	return 0;
}
char str_len(char str[])
{
	int i;
	for(i=0;i<99;i++)
	{
		if(str[i]=='\0')
		break;
	}
	return i;
}
